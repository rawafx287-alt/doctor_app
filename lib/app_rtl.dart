import 'dart:ui' show TextDirection;

/// Canonical RTL layout direction for Kurdish UI.
/// Use this instead of repeating [TextDirection.rtl] (must be lowercase `rtl` in Dart).
const TextDirection kRtlTextDirection = TextDirection.rtl;
